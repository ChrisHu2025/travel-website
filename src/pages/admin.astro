---
// src/pages/admin.astro
import { cmsConfig } from '../lib/cms/config';

export const prerender = true;

// 序列化配置
const serializedConfig = JSON.stringify({
  ...cmsConfig,
  load_config_file: false
});
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Content Management</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/cms.css" />
  <style>
    body { margin: 0; background: #eee; font-family: sans-serif; }
    #login-status {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%;
      background: rgba(255,255,255,0.9); z-index: 9999;
      display: flex; flex-direction: column; align-items: center; justify-content: center;
    }
    .spinner {
      width: 40px; height: 40px; border: 4px solid #ddd;
      border-top: 4px solid #0070f3; border-radius: 50%;
      animation: spin 1s linear infinite; margin-bottom: 20px;
    }
    @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
  </style>
</head>
<body>
  <div id="nc-root"></div>

  <script id="cms-config" type="application/json" is:inline set:html={serializedConfig}></script>

  <!-- 状态提示层（默认隐藏，收到Token后显示） -->
  <div id="login-status" style="display:none;">
    <div class="spinner"></div>
    <h3 id="status-text">正在验证身份...</h3>
  </div>

  <script is:inline>
    window.CMS_MANUAL_INIT = true;

    // ✅✅✅ 核心监听器：无条件执行 ✅✅✅
    window.addEventListener('message', (event) => {
      if (typeof event.data === 'string' && event.data.startsWith('authorization:github:success:')) {

        // 1. 立即显示 Loading 遮罩，防止用户重复点击
        const statusEl = document.getElementById('login-status');
        statusEl.style.display = 'flex';
        document.getElementById('status-text').innerText = '认证成功！正在进入后台...';

        console.log('🚀 收到 Token，准备跳转...');

        try {
          // 2. 解析 Token
          const jsonStr = event.data.match(/success:(.+)/)[1];
          const data = JSON.parse(jsonStr);

          if (data.token) {
            // 3. 🧹 清理可能存在的旧数据（防止干扰）
            localStorage.removeItem('netlify-cms-user');

            // 4. 🔑 构造 Hash 链接
            // Decap CMS 启动时会检查 URL 里的 #token=...
            // 如果存在，它会自动完成登录
            const newHash = `#token=${data.token}`;

            console.log('执行 Hash 注入:', newHash);

            // 5. 修改 URL 并强制刷新
            window.location.hash = newHash;
            window.location.reload();
          }
        } catch (e) {
          console.error('Login Failed:', e);
          alert('登录数据解析失败');
          statusEl.style.display = 'none';
        }
      }
    });
  </script>

  <script src="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/decap-cms.js"></script>

  <script is:inline>
    // 标准初始化
    window.addEventListener('load', function() {
      const configEl = document.getElementById('cms-config');
      if (!window.CMS) return;

      try {
        const config = JSON.parse(configEl.textContent);

        // 动态域名适配
        config.backend.base_url = window.location.origin;
        config.backend.auth_endpoint = '/api/decap-cms-github';

        CMS.init({ config });

      } catch (e) {
        console.error('Init Error:', e);
      }
    });
  </script>
</body>
</html>
