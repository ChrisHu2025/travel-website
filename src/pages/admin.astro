---
// src/pages/admin.astro
import '../styles/global.css';
// 从模块导入配置（请确保 src/lib/cms/config.ts 已正确定义并导出 cmsConfig）
import { cmsConfig } from '../lib/cms/config';

export const prerender = true;

// ====== 安全序列化配置（解决JSON解析错误的核心） ======
function serializeToSafeJSON(config) {
  // 1. 深度克隆，确保操作不影响原模块
  const configToSerialize = JSON.parse(JSON.stringify(config));

  // 2. 强制关键开关（双重保险）
  configToSerialize.load_config_file = false;

  // 3. 执行标准序列化
  let jsonString = JSON.stringify(configToSerialize);

  // 4. 修复所有可能导致 script 标签内解析失败的字符
  jsonString = jsonString
    .replace(/\u2028/g, '\\u2028') // 替换行分隔符
    .replace(/\u2029/g, '\\u2029') // 替换段落分隔符
    .replace(/</g, '\\u003c')      // 转义 <
    .replace(/>/g, '\\u003e')      // 转义 >
    .replace(/&/g, '\\u0026')      // 转义 &
    .replace(/'/g, '\\u0027');     // 转义单引号（防御性）

  return jsonString;
}

const serializedConfig = serializeToSafeJSON(cmsConfig);
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>内容管理系统 - Explore China</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/cms.css" />
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f5f5;
    }
    #nc-root { min-height: 100vh; }
    .cms-loading {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%;
      display: flex; flex-direction: column; align-items: center; justify-content: center;
      background: white; z-index: 9999; transition: opacity 0.3s;
    }
    .cms-loading.hidden { opacity: 0; pointer-events: none; }
    .loading-spinner {
      width: 40px; height: 40px; border: 4px solid #f3f3f3; border-top: 4px solid #3498db;
      border-radius: 50%; animation: spin 1s linear infinite; margin-bottom: 16px;
    }
    @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    .loading-text { color: #666; font-size: 16px; }
    .cms-error {
      position: fixed; top: 20px; left: 50%; transform: translateX(-50%);
      background: #ffebee; color: #c62828; padding: 12px 24px; border-radius: 4px;
      border-left: 4px solid #c62828; max-width: 90%; z-index: 10000; display: none;
    }
  </style>
</head>
<body>
  <div id="nc-root"></div>

  <!-- ✅ 使用 set:html 安全注入已转义的JSON配置 -->
  <script id="cms-config-data" type="application/json" set:html={serializedConfig}></script>

  <div class="cms-loading" id="js-loading">
    <div class="loading-spinner"></div>
    <div class="loading-text">正在加载内容编辑器...</div>
  </div>
  <div class="cms-error" id="js-error"></div>

  <script is:inline src="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/decap-cms.js"></script>

  <script is:inline>
    window.CMS_MANUAL_INIT = true;
    document.addEventListener('DOMContentLoaded', function() {
      const loadingEl = document.getElementById('js-loading');
      const errorEl = document.getElementById('js-error');

      let CONFIG;
      try {
        const configEl = document.getElementById('cms-config-data');
        CONFIG = JSON.parse(configEl.textContent); // 现在这里一定是合法的JSON
        console.log('✅ CMS 配置加载成功');
      } catch (e) {
        console.error('❌ 配置解析失败:', e, '原始内容:', configEl?.textContent?.substring(0, 200));
        showError('配置解析失败，请检查控制台。');
        return;
      }

      let attempts = 0;
      const MAX_ATTEMPTS = 30;
      function initCMS() {
        attempts++;
        if (!window.CMS) {
          if (attempts >= MAX_ATTEMPTS) {
            showError('加载 CMS 库超时，请刷新页面');
            return;
          }
          setTimeout(initCMS, 100);
          return;
        }
        try {
          CMS.init({ config: CONFIG });
          // 注册成功回调
          CMS.registerEventListener({
            name: 'init',
            handler: () => {
              loadingEl.classList.add('hidden');
              setTimeout(() => loadingEl.style.display = 'none', 300);
              console.log('✅ CMS 初始化完成');
            }
          });
          // 注册错误监听
          CMS.registerEventListener({
            name: 'error',
            handler: (error) => { showError('CMS 错误: ' + (error.message || '未知错误')); }
          });
        } catch (e) {
          showError('CMS 初始化失败: ' + e.message);
        }
      }
      function showError(msg) {
        if (errorEl) {
          errorEl.textContent = msg;
          errorEl.style.display = 'block';
        }
        console.error('CMS Error:', msg);
        loadingEl.style.display = 'none';
      }
      initCMS();
    });
  </script>
</body>
</html>
