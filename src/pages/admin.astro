---
// src/pages/admin.astro
import '../styles/global.css';
import { cmsConfig } from '../lib/cms/config';

export const prerender = true;

// ====== 安全序列化配置（仅用于 <script id="cms-config">） ======
function serializeToSafeJSON(config) {
  const configToSerialize = JSON.parse(JSON.stringify({
    ...config,
    load_config_file: false // 强制关闭远程 config.yml 加载
  }));

  let jsonString = JSON.stringify(configToSerialize, null, 0); // 不要缩进，减少体积

  // 转义可能破坏 script 标签的字符（虽然 JSON 通常安全，但防御性处理）
  jsonString = jsonString
    .replace(/\u2028/g, '\\u2028')
    .replace(/\u2029/g, '\\u2029')
    .replace(/</g, '\\u003c')
    .replace(/>/g, '\\u003e');

  return jsonString;
}

const serializedConfig = serializeToSafeJSON(cmsConfig);
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>内容管理系统 - Explore China</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/cms.css" />
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f5f5;
    }
    #nc-root { min-height: 100vh; }
    .cms-loading {
      position: fixed; top: 0; left: 0; width: 100%; height: 100%;
      display: flex; flex-direction: column; align-items: center; justify-content: center;
      background: white; z-index: 9999; transition: opacity 0.3s;
    }
    .cms-loading.hidden { opacity: 0; pointer-events: none; }
    .loading-spinner {
      width: 40px; height: 40px; border: 4px solid #f3f3f3; border-top: 4px solid #3498db;
      border-radius: 50%; animation: spin 1s linear infinite; margin-bottom: 16px;
    }
    @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
    .loading-text { color: #666; font-size: 16px; }
    .cms-error {
      position: fixed; top: 20px; left: 50%; transform: translateX(-50%);
      background: #ffebee; color: #c62828; padding: 12px 24px; border-radius: 4px;
      border-left: 4px solid #c62828; max-width: 90%; z-index: 10000; display: none;
    }
  </style>
</head>
<body>
  <div id="nc-root"></div>

  <!-- ✅ 关键修改：使用官方支持的 <script id="cms-config">（无 type 属性） -->
  <script id="cms-config" set:html={serializedConfig}></script>

  <div class="cms-loading" id="js-loading">
    <div class="loading-spinner"></div>
    <div class="loading-text">正在加载内容编辑器...</div>
  </div>
  <div class="cms-error" id="js-error"></div>

  <!-- ✅ 手动初始化开关 -->
  <script is:inline>
    window.CMS_MANUAL_INIT = true;
  </script>

  <!-- ✅ 先加载 CMS 库 -->
  <script is:inline src="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/decap-cms.js"></script>

  <!-- ✅ 再执行初始化和事件监听 -->
  <script is:inline>
    document.addEventListener('DOMContentLoaded', function() {
      const loadingEl = document.getElementById('js-loading');
      const errorEl = document.getElementById('js-error');

      function showError(msg) {
        if (errorEl) {
          errorEl.textContent = msg;
          errorEl.style.display = 'block';
        }
        console.error('CMS Error:', msg);
        loadingEl.style.display = 'none';
      }

      // 等待 CMS 全局对象就绪
      let attempts = 0;
      const MAX_ATTEMPTS = 30;
      function initCMS() {
        attempts++;
        if (!window.CMS) {
          if (attempts >= MAX_ATTEMPTS) {
            showError('加载 CMS 库超时，请刷新页面');
            return;
          }
          setTimeout(initCMS, 100);
          return;
        }

        try {
          // ✅ 关键：不再传 config！CMS 已从 <script id="cms-config"> 自动读取
          CMS.init();

          // ✅ 修正事件名：'init' → 'initialized'
          CMS.registerEventListener({
            name: 'initialized',
            handler: () => {
              loadingEl.classList.add('hidden');
              setTimeout(() => loadingEl.style.display = 'none', 300);
              console.log('✅ CMS 初始化完成');
            }
          });

          CMS.registerEventListener({
            name: 'error',
            handler: (error) => {
              showError('CMS 错误: ' + (error.message || '未知错误'));
            }
          });
        } catch (e) {
          showError('CMS 初始化失败: ' + e.message);
        }
      }

      initCMS();
    });
  </script>
</body>
</html>
