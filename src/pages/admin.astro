---
// src/pages/admin.astro
import { cmsConfig } from '../lib/cms/config';

export const prerender = true;

// 序列化配置
const serializedConfig = JSON.stringify({
  ...cmsConfig,
  load_config_file: false
});
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Content Manager</title>
  <!-- 引入官方样式 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/cms.css" />
</head>
<body>
  <div id="nc-root"></div>

  <!-- 1. 注入原始配置 -->
  <script id="cms-config" type="application/json" is:inline set:html={serializedConfig}></script>

  <!-- 2. 设置手动初始化标志 -->
  <script is:inline>
    window.CMS_MANUAL_INIT = true;
  </script>

  <!-- 3. 加载 CMS 库 -->
  <script src="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/decap-cms.js"></script>

  <!-- 4. 标准初始化脚本 (关键修正：动态 base_url) -->
  <script is:inline>
    // 确保 DOM 和 CMS 库都已加载
    function initCMS() {
      const configEl = document.getElementById('cms-config');
      if (!window.CMS || !configEl) return;

      try {
        const config = JSON.parse(configEl.textContent);

        // ✅✅✅ 关键修正：将 base_url 强制设为当前页面的 Origin ✅✅✅
        // 这样无论你是 www、非www、还是 localhost，Decap CMS 都会认为 API 是“自己人”
        config.backend.base_url = window.location.origin;

        console.log('🚀 初始化 CMS, Base URL:', config.backend.base_url);

        CMS.init({ config });
      } catch (e) {
        console.error('CMS Init Failed:', e);
        document.body.innerHTML = '<p style="color:red;padding:20px">配置错误: ' + e.message + '</p>';
      }
    }

    // 监听脚本加载完成
    if (document.readyState === 'complete') {
      initCMS();
    } else {
      window.addEventListener('load', initCMS);
    }
  </script>
</body>
</html>
