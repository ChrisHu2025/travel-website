---
// src/pages/admin.astro
import { cmsConfig } from '../lib/cms/config';

export const prerender = true;

// 序列化配置为内联 JSON，并禁用 config.yml 加载
const serializedConfig = JSON.stringify({
  ...cmsConfig,
  load_config_file: false
});
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>内容管理系统 - Explore China</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/cms.css" />
  <style>
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f5f5;
    }
    .cms-loading {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: white;
      z-index: 9999;
      transition: opacity 0.3s;
    }
    .cms-loading.hidden {
      opacity: 0;
      pointer-events: none;
    }
    .loading-spinner {
      width: 40px;
      height: 40px;
      border: 4px solid #f3f3f3;
      border-top: 4px solid #3498db;
      border-radius: 50%;
      animation: spin 1s linear infinite;
      margin-bottom: 16px;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
    .loading-text {
      color: #666;
      font-size: 16px;
    }
    .cms-error {
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      background: #ffebee;
      color: #c62828;
      padding: 12px 24px;
      border-radius: 4px;
      border-left: 4px solid #c62828;
      max-width: 90%;
      z-index: 10000;
      display: none;
    }
  </style>
</head>
<body>
  <!-- ✅ 防止 Astro 添加 data-astro-cid -->
  <div id="nc-root" is:inline></div>

  <!-- ✅ 配置脚本注入 -->
  <script id="cms-config" is:inline>
    {serializedConfig}
  </script>

  <!-- ✅ Loading UI -->
  <div class="cms-loading" id="js-loading">
    <div class="loading-spinner"></div>
    <div class="loading-text">正在加载内容编辑器...</div>
  </div>
  <div class="cms-error" id="js-error"></div>

  <!-- ✅ 1. 设置手动初始化标志 (必须最先执行) -->
  <script is:inline>
    window.CMS_MANUAL_INIT = true;
  </script>

  <!-- ✅ 2. 加载 Decap CMS 库 (添加 defer 属性以不阻塞渲染) -->
  <script src="https://cdn.jsdelivr.net/npm/decap-cms@3.9.0/dist/decap-cms.js" defer></script>

  <!-- ✅ 3. 初始化逻辑 (等待 load 事件确保 CMS 库已完全就绪) -->
  <script is:inline>
    window.addEventListener('load', function () {
      const loadingEl = document.getElementById('js-loading');
      const errorEl = document.getElementById('js-error');
      const configEl = document.getElementById('cms-config');

      function showError(msg) {
        console.error('CMS Error:', msg);
        if (errorEl) {
          errorEl.textContent = msg;
          errorEl.style.display = 'block';
        }
        if (loadingEl) {
          loadingEl.style.display = 'none';
        }
      }

      // 检查 CMS 库是否加载
      if (!window.CMS) {
        showError('Decap CMS 库未能加载，请检查网络连接或 CDN 状态。');
        return;
      }

      // 读取并解析配置
      let CONFIG;
      try {
        if (!configEl) throw new Error('未找到配置脚本 #cms-config');
        CONFIG = JSON.parse(configEl.textContent || configEl.innerText);
        console.log('✅ CMS 配置解析成功');
      } catch (e) {
        showError('CMS 配置解析失败: ' + e.message);
        return;
      }

      // 执行初始化
      try {
        console.log('🚀 开始初始化 Decap CMS...');
        CMS.init({ config: CONFIG });

        // 成功后隐藏 loading
        if (loadingEl) {
          // 稍微延迟一下以确保 UI 渲染完成
          setTimeout(() => {
            loadingEl.classList.add('hidden');
            setTimeout(() => {
              if (loadingEl) loadingEl.style.display = 'none';
            }, 300);
          }, 500);
        }
      } catch (e) {
        showError('CMS 初始化执行异常: ' + (e.message || e.toString()));
      }
    });
  </script>
</body>
</html>
