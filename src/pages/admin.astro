---
// src/pages/admin.astro
import '../styles/global.css';
export const prerender = true;
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>内容管理系统 - 旅游网站</title>
  <link rel="stylesheet" href="https://unpkg.com/decap-cms@3.9.0/dist/cms.css">
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f8f9fa;
    }
    .loading-container {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      text-align: center;
    }
    .loading-spinner {
      border: 4px solid rgba(0, 0, 0, 0.1);
      border-radius: 50%;
      border-top: 4px solid #3498db;
      width: 40px;
      height: 40px;
      animation: spin 1s linear infinite;
      margin: 0 auto 20px;
    }
    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <!-- ✅ Decap CMS 标准挂载点 -->
  <div id="nc-root"></div>

  <div class="loading-container" id="cms-loading">
    <div class="loading-spinner"></div>
    <p id="loading-text">正在加载内容管理系统...</p>
  </div>

  <script src="https://unpkg.com/decap-cms@3.9.0/dist/decap-cms.js"></script>

  <script>
    window.CMS_MANUAL_INIT = true;

    document.addEventListener('DOMContentLoaded', function() {
      console.log('监听页面加载完成，准备初始化 CMS...');

      // ✅ 新增：标志位，防止重复移除
      let loadingRemoved = false;

      // ✅ 新增：安全移除函数（可被多个地方调用）
      function removeLoading(message = null) {
        if (loadingRemoved) return; // 防止重复操作
        loadingRemoved = true;

        const loadingEl = document.getElementById('cms-loading');
        if (loadingEl && loadingEl.parentNode) {
          if (message) {
            // 显示提示信息
            loadingEl.querySelector('#loading-text').textContent = message;
            // 稍作延迟再移除，让用户看到提示
            setTimeout(() => {
              if (loadingEl.parentNode) {
                loadingEl.parentNode.removeChild(loadingEl);
              }
            }, 2000);
          } else {
            // 正常移除
            loadingEl.parentNode.removeChild(loadingEl);
          }
        }
      }

      function waitForCMS() {
        if (typeof window.CMS === 'undefined') {
          setTimeout(waitForCMS, 100);
          return;
        }

        try {
          console.log('✅ CMS 已加载，开始手动初始化...');

          window.CMS.init({
            config: {
              backend: {
                name: 'github',
                repo: 'ChrisHu2025/travel-website',
                branch: 'main',
                auth_scope: 'public_repo',
                auth_type: 'implicit',
                client_id: 'Ov23liJds0MQcgZsIeob'
              },
              load_config_file: false
            }
          });

          // 注册预览样式（如有需要再启用）
          // window.CMS.registerPreviewStyle('/css/cms-preview.css');

          // ✅ 监听 init 事件：正常流程移除 loading
          window.CMS.registerEventListener({
            name: 'init',
            handler: () => {
              console.log('🎉 CMS UI 已就绪');
              removeLoading(); // 正常移除
            }
          });

          // ✅ 新增：安全超时（5秒后备）
          setTimeout(() => {
            if (!loadingRemoved) {
              console.warn('⚠️ CMS init 事件未在5秒内触发，启动超时保护');
              removeLoading('加载时间较长，请稍候或刷新页面');
            }
          }, 5000);

        } catch (error) {
          console.error('❌ CMS 手动初始化失败:', error);
          // 错误时立即更新提示（不依赖超时）
          const loadingEl = document.getElementById('cms-loading');
          if (loadingEl) {
            loadingEl.innerHTML = `
              <div style="text-align: center; padding: 2rem;">
                <h2 style="color: #e53e3e; margin-bottom: 1rem;">加载失败</h2>
                <p style="margin-bottom: 1rem;">内容管理系统初始化失败，请尝试以下方法：</p>
                <ul style="text-align: left; max-width: 500px; margin: 0 auto 1rem;">
                  <li>检查网络连接</li>
                  <li>清除浏览器缓存后重试</li>
                </ul>
                <button onclick="location.reload()" style="padding: 8px 16px; background: #3498db; color: white; border: none; border-radius: 4px; cursor: pointer;">
                  刷新页面
                </button>
                <p style="margin-top: 1rem; color: #666;">错误详情: ${error.message}</p>
              </div>
            `;
            loadingRemoved = true; // 标记已处理
          }
        }
      }

      waitForCMS();
    });
  </script>
</body>
</html>
